import styled from "styled-components";

const Icon = styled.img`
  height: 16px;
  margin-right: 8px;
  transition: 0.3s;
  filter: invert(100%) sepia(100%) saturate(0%) hue-rotate(114deg) brightness(105%) contrast(101%);
`;

export default Icon;
